{
  "name": "@activepieces/piece-llm-orchestrator",
  "version": "0.0.1",
  "description": "Advanced LLM orchestration and workflow management",
  "main": "src/index.ts",
  "type": "commonjs",
  "scripts": {
    "build": "tsup src/index.ts --format cjs --dts",
    "dev": "tsup src/index.ts --format cjs --watch --onSuccess \"node dist/index.js\"",
    "test": "jest"
  },
  "dependencies": {
    "openai": "^4.20.1",
    "@anthropic-ai/sdk": "^0.24.3",
    "uuid": "^9.0.0",
    "zod": "^3.22.4"
  },
  "devDependencies": {
    "@types/uuid": "^9.0.0"
  }
}